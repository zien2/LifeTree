# Base URL for frontend (use your domain in production)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# JWT secret for signing tokens
JWT_SECRET=replace_with_strong_random_secret

# AI (Aliyun Bailian)
AI_API_KEY=replace_with_bailian_key

# MySQL connection
# Example: mysql://root:password@127.0.0.1:3306/life-db
DATABASE_URL=

# SMTP for email verification (QQ example)
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=1561588898@qq.com
SMTP_PASS=replace_with_qq_smtp_authorization_code


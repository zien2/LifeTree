version: "3.9"
services:
  app:
    build: .
    container_name: lifetree-app
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always
    # 如果你需要在容器内执行 prisma 命令访问外部数据库，请保证 DATABASE_URL 指向可达地址


